#include <stdio.h>
/**
 * main - main code
 * Description: "Print single b10 digit number"
 * Return: Always 0
 */
int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
	putchar(n +  48);
	}
	putchar('\n');
	return (0);
}
